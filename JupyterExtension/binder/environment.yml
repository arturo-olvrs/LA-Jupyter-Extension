# a mybinder.org-ready environment for demoing LA_Jupyter_Extension
# this environment may also be used locally on Linux/MacOS/Windows, e.g.
#
#   conda env update --file binder/environment.yml
#   conda activate LA-Jupyter-Extension-demo
#
name: LA-Jupyter-Extension-demo

channels:
  - conda-forge

dependencies:
  # runtime dependencies
  - python >=3.13,<3.14.0a0
  - jupyterlab >=4.0.0,<5
  # labextension build dependencies
  - nodejs >=22,<23
  - pip
  - wheel
  # additional packages for demos
  # - ipywidgets
